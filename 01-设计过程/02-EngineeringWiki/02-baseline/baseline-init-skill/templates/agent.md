# 工程现状分析报告 (Agent Analysis)

> 本文档由基线初始化 Skill 在 **阶段一 · Step 1.1** 自动生成。
> 标记 `[INFERRED]` 的内容为基于代码逆向推导，未经业务方确认。

---

## 1. 工程画像 (Project Portrait)

| 维度         | 内容                                        |
| ------------ | ------------------------------------------- |
| **项目名称** | `{project_name}`                            |
| **核心语言** | `{language}` (e.g., Java 17 / TypeScript 5) |
| **框架版本** | `{framework}` (e.g., Spring Boot 3.2.x)     |
| **构建工具** | `{build_tool}` (e.g., Maven 3.9 / pnpm 8)   |
| **包管理**   | `{package_manager}`                         |
| **当前状态** | `{status}` (遗留系统 / 新启动 / 维护期)     |
| **代码规模** | `{loc}` 行代码，`{modules}` 个核心模块      |
| **仓库信息** | `{repo_url}` / 分支策略: `{branch_model}`   |

---

## 2. 目录结构扫描 (Directory Structure Scan)

> 扫描当前根目录层级，识别关键模块并注释其职责。

```
project-root/
├── src/                     # 源代码
│   ├── main/
│   │   ├── java/            # Java 源码
│   │   └── resources/       # 配置资源
│   └── test/                # 测试代码
├── docs/                    # 文档资产
├── scripts/                 # 部署/构建脚本
├── config/                  # 外部配置
└── {other}/                 # [描述]
```

**模块清单**：

| 模块名           | 路径              | 职责描述         | 依赖关系         |
| ---------------- | ----------------- | ---------------- | ---------------- |
| `{module_name}`  | `{module_path}`   | `{description}`  | `{dependencies}` |

---

## 3. 技术栈依赖分析 (Dependency Analysis)

### 3.1 核心依赖

| 类别         | 名称                | 版本       | 用途              | 活跃度 |
| ------------ | ------------------- | ---------- | ----------------- | ------ |
| 核心框架     | `{name}`            | `{ver}`    | `{purpose}`       | ✅ / ⚠️ |
| 数据库驱动   | `{name}`            | `{ver}`    | `{purpose}`       | ✅ / ⚠️ |
| ORM          | `{name}`            | `{ver}`    | `{purpose}`       | ✅ / ⚠️ |
| 中间件       | `{name}`            | `{ver}`    | `{purpose}`       | ✅ / ⚠️ |
| 安全框架     | `{name}`            | `{ver}`    | `{purpose}`       | ✅ / ⚠️ |
| 工具库       | `{name}`            | `{ver}`    | `{purpose}`       | ✅ / ⚠️ |

### 3.2 外部服务依赖

| 服务名称     | 类型               | 联通方式           | 强/弱依赖  |
| ------------ | ------------------ | ------------------ | ---------- |
| `{service}`  | `{type}`           | `{protocol}`       | `{level}`  |

---

## 4. 现有文档资产盘点 (Documentation Audit)

| 文档路径          | 类型               | 最后更新     | 质量评估         | 备注            |
| ----------------- | ------------------ | ------------ | ---------------- | --------------- |
| `{doc_path}`      | `{doc_type}`       | `{date}`     | ⭐⭐⭐ / ⭐⭐ / ⭐   | `{notes}`       |

---

## 5. 风险与改进建议 (Risk & Recommendations)

### 5.1 识别风险

| #  | 风险项                | 严重程度       | 描述                     |
| -- | --------------------- | -------------- | ------------------------ |
| 1  | `{risk}`              | 🔴 高 / 🟡 中 / 🟢 低 | `{description}`          |

### 5.2 初始化路径建议

> 基于工程现状，推荐以下初始化优先级：

1. **P0 (立即)**: {建议}
2. **P1 (本周)**: {建议}
3. **P2 (规划中)**: {建议}
