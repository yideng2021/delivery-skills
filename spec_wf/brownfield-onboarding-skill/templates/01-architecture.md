# 架构认知基线 · {{repo_name}}

> 由 CodeGraph + GitNexus 双源提炼。**机器负责准确，AI 负责可理解。**
> 生成基线 commit：`{{commit}}`。细节请活查询 MCP，勿当永久真相。
> 信号来源标注：`[包结构]` 实际目录 · `[社区]` GitNexus 社区 · `[推断]` AI 推断待验证

---

## 一、分层架构

{{分层说明 + 代码示意框图：controller→service→dao→entity 等}}

## 二、业务域地图（核心产出 · 人审对象）

> 将 {{N}} 个机器社区 + {{M}} 个真实包，AI 收敛为 {{K}} 个顶层业务域。
> **双源交叉验证**：业务域 = GitNexus 社区 ∩ 包结构；单源不下结论。

### 主线域

| 业务域 | 包 | 社区规模 | 核心入口（Controller/模块） | 职责 |
|---|---|---|---|---|
| {{域}} | {{包}} | {{n}} | {{入口}} | {{一句话职责}} |

### 支撑/横切域

| 业务域 | 包 | 社区规模 | 核心入口 | 职责 |
|---|---|---|---|---|
| {{域}} | {{包}} | {{n}} | {{入口}} | {{职责}} |

## 三、业务主线（推断，待 Process 验证）

> `[推断]` 标注，须用活查询核对。

```
{{阶段1}} → {{阶段2}} → ... → {{阶段n}}
横切：{{横切域列表}}
```

## 四、噪声剔除说明

> 社区按命名聚类会产生伪业务域，已剔除。方法：社区"大"≠"重要"，按是否承载业务语义二次过滤。

| 伪域 | 规模 | 真相 |
|---|---|---|
| {{Impl/Dto/Uuid 等}} | {{n}} | {{命名聚类/技术工具类，非业务域}} |

## 五、活查询入口

| 想知道 | 查询方式 |
|---|---|
| 某域具体调用链 | `codegraph_explore("{{域关键词}}")` |
| 某符号 360° | `gitnexus context(name)` |
| 改动影响面 | `codegraph_impact` + `gitnexus impact` |
| 域内全部类 | `cypher: MEMBER_OF Community {heuristicLabel}` |
