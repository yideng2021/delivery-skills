<!-- snapshot
generated_at: {{ISO8601}}
pin_commit: {{commit}}
generator: architecture-guide
scope: {{域名}}
freshness: 教学用途。编程真理以活查询当前源码为准;本快照可能滞后于代码。
-->

# 业务域深描 · {{域名}}（{{中文业务名}}）

> 本域为 `{{commit}}` 时点快照。所属包 `{{包路径}}`；导览总览见 [`00-index.md`](00-index.md)。
> 每个断言带 `文件:行号`，可核对源码。

---

## 一、域职责

{{一段话:本域负责什么}}。
**边界**:管 {{...}}；不管 {{...}}（归 {{相邻域}}）。

## 二、核心实体与状态机

**核心实体**:{{Entity1}}（{{文件}}）、{{Entity2}}…

**状态机**（来自枚举 {{XxxStatus}} / {{XxxFlowStatus}}，{{文件:行号}}）:

```
{{状态A}} → {{状态B}} → {{状态C}}
         ↘ {{旁路状态}}
```

## 三、主线流程（多条）

### 流程 1:{{流程名}}
- **入口**:{{Controller.方法}}（{{文件:行号}}）
- **链路**:{{动作}} →（{{状态迁移}}）→ {{落点}}
- **关键分支**:{{条件 → 分支结果}}（{{文件:行号}}）
- **驱动机制**:{{同步 / 审批流回调 / MQ / feign}}

### 流程 2:{{流程名}}
{{同上结构}}

## 四、域间依赖

| 方向 | 对端域 | 用途 | 位置 |
|---|---|---|---|
| 调用 → | {{域}} | {{做什么}} | {{文件:行号}} |
| ← 被调用 | {{域}} | {{做什么}} | {{文件:行号}} |

## 五、跨域 / 跨系统触点

| 触点 | 方式 | 位置 |
|---|---|---|
| {{SAP / 仓储 / 用户中心 …}} | {{feign / Kafka / 定时}} | {{文件:行号}} |

## 六、关键约束与技术债

- 约束:{{校验规则 / 事务边界 / 幂等}}（{{文件:行号}}）
- 技术债 / 坑:{{已知问题}}（{{文件:行号}}）

## 七、测试缺口

- ⚠️ {{无覆盖测试的关键方法}}（{{文件:行号}}，来自 explore blast radius）

## 八、深入活查询

- 本域全部类:`cypher: MEMBER_OF Community {heuristicLabel:"{{社区标签}}"}`
- 某流程调用栈:`codegraph_explore("{{Controller/ServiceImpl} {动作}}")`
- 改动影响:`codegraph_impact("{{核心符号}}")`
