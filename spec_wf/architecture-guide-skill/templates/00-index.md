<!-- snapshot
generated_at: {{ISO8601}}
pin_commit: {{commit}}
generator: architecture-guide
scope: index
freshness: 教学用途。编程真理以活查询当前源码为准;本快照可能滞后于代码。
-->

# 架构导览 · {{repo_name}}

> 本导览为 `{{commit}}` 时点的架构快照,供**学习 / 交接 / 介绍**;细节以源码为准。
> 目录(业务域地图)见 [`../01-architecture.md`](../01-architecture.md);本目录是其展开的厚导览。

---

## 一、项目一句话

{{这个系统是做什么的 —— 业务定位 + 技术栈一句话}}

## 二、跨域主线全景（业务主干全链路）

> 把各核心域的主线串成端到端业务流。每个节点链到对应 `domain-*.md`。

```
{{阶段1(域)}} → {{阶段2(域)}} → ... → {{阶段n(域)}}
横切:{{SAP / 权限 / 资质 ...}}
```

| 阶段 | 业务域 | 一句话 | 深描 |
|---|---|---|---|
| {{n}} | {{域}} | {{该阶段做什么}} | [domain-{{域}}.md](domain-{{域}}.md) |

## 三、阅读路径（建议)

- 想懂**整体业务**:看 §二 全景 → 按顺序读各 `domain-*.md` 的"域职责"与"主线流程"。
- 想懂**某块功能**:直接读对应 `domain-<域>.md`。
- 想懂**改动影响**:读目标域的"域间依赖 / 跨域触点",再活查询 `codegraph_impact` 核对。

## 四、核心域索引（深描)

| 域 | 职责 | 文件 |
|---|---|---|
| {{核心域}} | {{一句话}} | [domain-{{域}}.md](domain-{{域}}.md) |

## 五、支撑 / 横切域（浅描)

> 工具性/集成性域,不单独成文件;需深入时按锚点活查询。

### 支撑域:{{域名}}
- 职责:{{一句话}}
- 核心入口:{{Controller/模块}}（{{文件:行号}}）
- 与主线关系:{{被哪些核心域调用 / 提供什么能力}}
- 深入:`codegraph_explore("{{域关键词}}")`

## 六、快照与维护

- 本导览 pin `{{commit}}`。代码演进后用 `gitnexus detect_changes` 定位变更域,**只重跑受影响的 `domain-*.md`**。
- 维护纪律见 architecture-guide skill `references/snapshot-discipline.md`。
