# 测试用例文档 — {模块名称}

> **文档编号**: `TC_{序号}_{模块名}`
> **所属模块**: `{模块名称}`
> **关联路由**: `{路由路径，如 /#/factor-management}`
> **关联验收标准**: `{AC 编号列表，如 AC#1, AC#2，若无则标注"无上游 AC"}`
> **创建日期**: `{日期}`

---

## 1. 模块概述

> 用 2-3 句话概括该模块的核心业务功能。

{模块功能描述}

---

## 2. 前置条件

| #  | 条件                     | 说明                                      |
| -- | ------------------------ | ----------------------------------------- |
| 1  | 被测服务已启动            | `{BASE_URL}` 可正常访问                   |
| 2  | 浏览器已打开              | `playwright-cli open` 已执行               |
| 3  | 依赖数据已就绪            | `{依赖模块}` 的正向用例已执行（若有依赖）  |

---

## 3. 正向用例 (Positive Cases)

### P01 — 页面加载验证

| 项目       | 内容                                                    |
| ---------- | ------------------------------------------------------- |
| **目的**   | 验证模块页面可正常加载，关键 UI 元素均已渲染             |
| **前置**   | 无                                                       |
| **步骤**   | ① `goto {URL}` → ② `snapshot` → ③ 检查关键元素 → ④ 截图 |
| **预期**   | 页面加载完成，标题/按钮/表格等核心元素可见                |
| **截图**   | `{NN}_{模块}_page_load.png`                              |

### P02 — 完整正向流程（新增/创建）

| 项目       | 内容                                                                      |
| ---------- | ------------------------------------------------------------------------- |
| **目的**   | 验证完整的新增流程：打开表单 → 填写数据 → 提交 → 验证成功                   |
| **前置**   | P01 页面已加载                                                             |
| **步骤**   | ① `click {新增按钮ref}` → ② `snapshot`（弹窗） → ③ `fill` 各字段 → ④ 截图（已填写状态） → ⑤ `click {保存ref}` → ⑥ `snapshot` → ⑦ 验证成功提示 → ⑧ 截图 |
| **预期**   | 提交成功，显示成功提示。列表中新增一条记录                                  |
| **截图**   | `{NN}_{模块}_add_form.png`、`{NN}_{模块}_add_success.png`                  |

### P03 — 查看/编辑已有数据

| 项目       | 内容                                                                      |
| ---------- | ------------------------------------------------------------------------- |
| **目的**   | 验证查看/编辑已有数据的流程                                                 |
| **前置**   | P02 已创建数据                                                             |
| **步骤**   | ① `snapshot` 获取列表行操作按钮 → ② `click {编辑ref}` → ③ `snapshot` → ④ 验证表单回填 → ⑤ 修改数据 → ⑥ `click {保存ref}` → ⑦ `snapshot` → ⑧ 截图 |
| **预期**   | 编辑成功，列表中数据已更新                                                  |
| **截图**   | `{NN}_{模块}_edit_form.png`、`{NN}_{模块}_edit_success.png`                |

### P04 — 删除数据

| 项目       | 内容                                                                      |
| ---------- | ------------------------------------------------------------------------- |
| **目的**   | 验证删除流程：点击删除 → 确认弹窗 → 数据移除                               |
| **前置**   | 存在可删除的测试数据                                                       |
| **步骤**   | ① `snapshot` → ② `click {删除ref}` → ③ `snapshot`（确认弹窗） → ④ 截图 → ⑤ `click {确认ref}` → ⑥ `snapshot` → ⑦ 验证数据已移除 → ⑧ 截图 |
| **预期**   | 删除成功，列表中该条记录消失                                                |
| **截图**   | `{NN}_{模块}_delete_confirm.png`、`{NN}_{模块}_delete_success.png`         |

---

## 4. 逆向用例 (Negative Cases)

### N01 — 空表单提交拦截

| 项目       | 内容                                                                   |
| ---------- | ---------------------------------------------------------------------- |
| **目的**   | 验证不填写任何字段直接提交时，前端校验是否生效                           |
| **前置**   | P01 页面已加载                                                          |
| **步骤**   | ① `click {新增ref}` → ② `snapshot` → ③ 不填写，直接 `click {保存ref}` → ④ `snapshot` → ⑤ 截图校验提示 |
| **预期**   | 所有必填字段显示校验错误提示，表单未提交                                 |
| **截图**   | `{NN}_{模块}_empty_validation.png`                                      |

### N02 — 格式非法输入

| 项目       | 内容                                                                   |
| ---------- | ---------------------------------------------------------------------- |
| **目的**   | 验证输入非法格式数据时，校验是否拦截                                     |
| **前置**   | P01 页面已加载                                                          |
| **步骤**   | ① `click {新增ref}` → ② `snapshot` → ③ 填入非法数据（如数字字段填字母、超长字符串、特殊字符等）→ ④ `click {保存ref}` → ⑤ `snapshot` → ⑥ 截图 |
| **预期**   | 显示对应的格式校验错误提示                                               |
| **截图**   | `{NN}_{模块}_invalid_format.png`                                        |

### N03 — 边界值验证

| 项目       | 内容                                                                   |
| ---------- | ---------------------------------------------------------------------- |
| **目的**   | 验证超长字符串、极端数值等边界值的处理                                   |
| **前置**   | P01 页面已加载                                                          |
| **步骤**   | ① `click {新增ref}` → ② `snapshot` → ③ 填入超长字符串（100+ 字符）→ ④ `click {保存ref}` → ⑤ 观察结果 → ⑥ 截图 |
| **预期**   | 输入框限制最大长度，或提示"超过最大长度限制"                              |
| **截图**   | `{NN}_{模块}_boundary_value.png`                                        |

### N04 — 取消操作验证

| 项目       | 内容                                                                   |
| ---------- | ---------------------------------------------------------------------- |
| **目的**   | 验证取消/关闭弹窗操作不会产生副作用                                     |
| **前置**   | P01 页面已加载                                                          |
| **步骤**   | ① `click {新增ref}` → ② `snapshot` → ③ 填写部分数据 → ④ `click {取消ref}` → ⑤ `snapshot` → ⑥ 确认无新数据被创建 → ⑦ 截图 |
| **预期**   | 弹窗关闭，列表数据无变化                                                 |
| **截图**   | `{NN}_{模块}_cancel_no_effect.png`                                      |

---

## 5. 用例依赖关系

```
{模块名}-P01 (页面加载)
  ├── {模块名}-P02 (新增) ← 为后续用例提供基础数据
  │     ├── {模块名}-P03 (编辑) ← 依赖 P02 创建的数据
  │     └── {模块名}-P04 (删除) ← 依赖 P02 创建的数据（最后执行）
  ├── {模块名}-N01 (空表单拦截)
  ├── {模块名}-N02 (格式非法)
  ├── {模块名}-N03 (边界值)
  └── {模块名}-N04 (取消操作)
```

> **执行顺序建议**：先正向（P01 → P02 → P03）→ 再逆向（N01 → N02 → N03 → N04）→ 最后清理（P04）

---

## 6. 截图清单

| 序号 | 截图文件名                          | 对应用例 | 场景说明           |
| ---- | ----------------------------------- | -------- | ------------------ |
| {NN} | `{NN}_{模块}_page_load.png`         | P01      | 页面初始加载       |
| {NN} | `{NN}_{模块}_add_form.png`          | P02      | 新增表单已填写     |
| {NN} | `{NN}_{模块}_add_success.png`       | P02      | 新增成功提示       |
| {NN} | `{NN}_{模块}_edit_form.png`         | P03      | 编辑表单回填       |
| {NN} | `{NN}_{模块}_edit_success.png`      | P03      | 编辑成功提示       |
| {NN} | `{NN}_{模块}_delete_confirm.png`    | P04      | 删除确认弹窗       |
| {NN} | `{NN}_{模块}_delete_success.png`    | P04      | 删除成功           |
| {NN} | `{NN}_{模块}_empty_validation.png`  | N01      | 空表单校验提示     |
| {NN} | `{NN}_{模块}_invalid_format.png`    | N02      | 格式校验提示       |
| {NN} | `{NN}_{模块}_boundary_value.png`    | N03      | 边界值测试         |
| {NN} | `{NN}_{模块}_cancel_no_effect.png`  | N04      | 取消操作无副作用   |
